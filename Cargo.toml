[package]
name = "minecrab"
version = "0.1.0"
edition = "2024"

[dependencies]
noise = "0.9"
raylib = { package = "sola-raylib", git = "https://github.com/Sophon96/sola-raylib.git", branch = "stable-patching", commit = "49a13f9", features = [ "sdl" ] }
